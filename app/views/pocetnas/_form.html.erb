<%= form_with(model: pocetna) do |form| %>
  <% if pocetna.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pocetna.errors.count, "error") %> prohibited this pocetna from being saved:</h2>

      <ul>
        <% pocetna.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
